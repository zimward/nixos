* modify the code in the way that its stated in the first comment of the input code.
* the input code is marked by the <input> tag. the context is in a <context> block you output must be a enclosed by an <answer> tag.
* don't output the code from the context tag, just the code from the input tag with the applied changes.
* remove the comment the changes where based on
